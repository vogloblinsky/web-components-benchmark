{
    "name": "hybrids-pascal-triangle",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "prebuild": "rm -rf dist && mkdir dist",
        "build": "../../node_modules/.bin/webpack",
        "postbuild": "cp index.html dist"
    },
    "author": "",
    "license": "MIT",
    "dependencies": {
        "hybrids": "^4.0.2"
    }
}
