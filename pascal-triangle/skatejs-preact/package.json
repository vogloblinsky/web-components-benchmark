{
    "private": true,
    "name": "pascal-triangle-skatejs-preact",
    "scripts": {
        "prebuild": "rm -rf dist && mkdir dist",
        "build": "node build.js",
        "postbuild": "find dist -type f -name '*.js' -execdir mv {} main.js ';' && node fix-build.js"
    },
    "dependencies": {
        "@skatejs/element-preact": "0.0.1",
        "preact": "^8.4.2",
        "skatejs": "^5.2.4"
    },
    "devDependencies": {
        "@babel/core": "^7.4.5",
        "@babel/plugin-proposal-class-properties": "^7.4.4",
        "@babel/preset-react": "^7.0.0",
        "parcel-bundler": "^1.12.3"
    }
}