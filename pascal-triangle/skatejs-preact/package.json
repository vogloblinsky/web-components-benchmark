{
    "private": true,
    "name": "pascal-triangle-skatejs-preact",
    "scripts": {
        "prebuild": "rm -rf dist && mkdir dist",
        "build": "parcel build src/index.html --detailed-report --no-source-maps --public-url ./"
    },
    "dependencies": {
        "@skatejs/element-preact": "0.0.1",
        "preact": "^8.4.2",
        "skatejs": "^5.2.4"
    },
    "devDependencies": {
        "@babel/core": "^7.2.2",
        "@babel/plugin-proposal-class-properties": "^7.2.3",
        "@babel/preset-react": "^7.0.0",
        "parcel-bundler": "^1.11.0"
    }
}
