{
    "private": true,
    "name": "pascal-triangle-skatejs-preact",
    "scripts": {
        "prebuild": "rm -rf dist && mkdir dist",
        "build": "node build.js",
        "postbuild": "sh fix-build.sh && node fix-build-index.js"
    },
    "dependencies": {
        "@skatejs/element-preact": "0.0.1",
        "preact": "^8.4.2",
        "skatejs": "^5.2.4"
    },
    "devDependencies": {
        "@babel/core": "^7.4.5"
    }
}
